<Window x:Class="HydrargyrumBaikal.AddMarkerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HydrargyrumBaikal"
        mc:Ignorable="d"
        Title="Добавление новой записи" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <Label Content="ID:" />
            <TextBox x:Name="IdTextBox" />
            
            <Label Content="Название города:" />
            <TextBox x:Name="CityNameTextBox" />

            <Label Content="Номер:" />
            <TextBox x:Name="NumberTextBox" />

            <Label Content="Долгота:" />
            <TextBox x:Name="LongitudeTextBox" />

            <Label Content="Широта:" />
            <TextBox x:Name="LatitudeTextBox" />

            <Label Content="Образец:" />
            <TextBox x:Name="SampleTextBox" />

            <Button Content="Добавить" Click="AddMarkerButton_Click" />
        </StackPanel>
    </Grid>
</Window>
